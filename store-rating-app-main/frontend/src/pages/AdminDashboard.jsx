import {useEffect,useState} from 'react';
import api from '../api';
import Header from '../components/Header';
import PasswordBox from '../components/PasswordBox';

const emptyUser={name:'',email:'',address:'',password:'',role:'USER'};const emptyStore={name:'',email:'',address:'',ownerId:''};
export default function AdminDashboard({user,onLogout}){
 const [stats,setStats]=useState({users:0,stores:0,ratings:0});const [users,setUsers]=useState([]);const [stores,setStores]=useState([]);const [owners,setOwners]=useState([]);const [userForm,setUserForm]=useState(emptyUser);const [storeForm,setStoreForm]=useState(emptyStore);const [filters,setFilters]=useState({name:'',email:'',address:'',role:''});const [storeFilter,setStoreFilter]=useState({name:'',email:'',address:''});const [msg,setMsg]=useState('');
 async function load(){const [a,b,c]=await Promise.all([api.get('/admin/dashboard'),api.get('/admin/users',{params:filters}),api.get('/admin/stores',{params:storeFilter})]);setStats(a.data);setUsers(b.data);setStores(c.data);setOwners(b.data.filter(x=>x.role==='OWNER'));}
 useEffect(()=>{load().catch(e=>setMsg(e.response?.data?.message||'Could not load data'))},[]);
 async function addUser(e){e.preventDefault();try{await api.post('/admin/users',userForm);setUserForm(emptyUser);setMsg('User added');load();}catch(e){setMsg(e.response?.data?.message||'Could not add user');}}
 async function addStore(e){e.preventDefault();try{await api.post('/admin/stores',storeForm);setStoreForm(emptyStore);setMsg('Store added');load();}catch(e){setMsg(e.response?.data?.message||'Could not add store');}}
 function changeUser(e){setUserForm({...userForm,[e.target.name]:e.target.value})} function changeStore(e){setStoreForm({...storeForm,[e.target.name]:e.target.value})}
 return <><Header user={user} onLogout={onLogout}/><main className="page"><h1>Admin Dashboard</h1>{msg&&<p>{msg}</p>}<div className="stats"><div><b>Total Users</b><strong>{stats.users}</strong></div><div><b>Total Stores</b><strong>{stats.stores}</strong></div><div><b>Total Ratings</b><strong>{stats.ratings}</strong></div></div><div className="forms"><form className="card" onSubmit={addUser}><h2>Add User</h2><input name="name" placeholder="Name (20-60 chars)" value={userForm.name} onChange={changeUser} required/><input name="email" type="email" placeholder="Email" value={userForm.email} onChange={changeUser} required/><input name="address" placeholder="Address" value={userForm.address} onChange={changeUser} required/><input name="password" type="password" placeholder="Password" value={userForm.password} onChange={changeUser} required/><select name="role" value={userForm.role} onChange={changeUser}><option>USER</option><option>ADMIN</option><option>OWNER</option></select><button>Add User</button></form><form className="card" onSubmit={addStore}><h2>Add Store</h2><input name="name" placeholder="Store name" value={storeForm.name} onChange={changeStore} required/><input name="email" type="email" placeholder="Store email" value={storeForm.email} onChange={changeStore} required/><input name="address" placeholder="Address" value={storeForm.address} onChange={changeStore} required/><select name="ownerId" value={storeForm.ownerId} onChange={changeStore}><option value="">No owner</option>{owners.map(o=><option key={o.id} value={o.id}>{o.name}</option>)}</select><button>Add Store</button></form></div><section className="card"><h2>Users</h2><div className="toolbar"><input placeholder="Name" value={filters.name} onChange={e=>setFilters({...filters,name:e.target.value})}/><input placeholder="Email" value={filters.email} onChange={e=>setFilters({...filters,email:e.target.value})}/><input placeholder="Address" value={filters.address} onChange={e=>setFilters({...filters,address:e.target.value})}/><select value={filters.role} onChange={e=>setFilters({...filters,role:e.target.value})}><option value="">All roles</option><option>USER</option><option>ADMIN</option><option>OWNER</option></select><button onClick={load}>Filter</button></div><table><thead><tr><th>Name</th><th>Email</th><th>Address</th><th>Role</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.name}</td><td>{u.email}</td><td>{u.address}</td><td>{u.role}</td></tr>)}</tbody></table></section><section className="card"><h2>Stores</h2><div className="toolbar"><input placeholder="Name" value={storeFilter.name} onChange={e=>setStoreFilter({...storeFilter,name:e.target.value})}/><input placeholder="Email" value={storeFilter.email} onChange={e=>setStoreFilter({...storeFilter,email:e.target.value})}/><input placeholder="Address" value={storeFilter.address} onChange={e=>setStoreFilter({...storeFilter,address:e.target.value})}/><button onClick={load}>Filter</button></div><table><thead><tr><th>Name</th><th>Email</th><th>Address</th><th>Rating</th></tr></thead><tbody>{stores.map(s=><tr key={s.id}><td>{s.name}</td><td>{s.email}</td><td>{s.address}</td><td>⭐ {s.average_rating}</td></tr>)}</tbody></table></section><PasswordBox/></main></>;
}
